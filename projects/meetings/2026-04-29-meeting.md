# Meeting — 2026-04-29
**Agenda**: Semak Workflow + Security Audit BFM2026
**Agents Hadir**: DEV, SECURITY, OPS, QA, DESIGN, DOC, RESEARCH, DATA, PM, STRATEGY

## Input Agents

### DEV
- Struktur kod baik, PDO prepared statements digunakan
- CSRF token ada pada login
- `login.php` define `getDB()` sendiri — perlu consolidate dengan database.php

### SECURITY — 4 Critical Flags
1. **API endpoints tanpa auth** — `/api/*.php` boleh diakses tanpa login, CORS `*`
2. **Fail maintenance terdedah** — `deploy.php`, `aggressive_cleanup.php`, `export_database_structure.php`, `system_check.php`, `rollback_technical_admin.php` etc. accessible tanpa auth
3. **File upload extension** — `pathinfo($file['name'], PATHINFO_EXTENSION)` ambil dari user input
4. **Tiada .htaccess** — folder listing mungkin aktif, config/ dan uploads/ terdedah

### OPS
- Apache24 + XAMPP MySQL running stable
- secrets.php perlu diasingkan antara local dan production
- backups_technical_admin/ folder ada di htdocs root

### QA
- Login workflow OK, CSRF implemented
- Role-based access dalam auth.php solid
- API tiada requireLogin()

## Keputusan
Sistem BELUM selamat untuk production. 4 isu kritikal perlu fix.

## Action Items
- [ ] Pindah/protect fail maintenance di root — DEV
- [ ] Tambah auth check pada API endpoints — DEV
- [ ] Buat .htaccess — protect config/, uploads/, block listing — OPS
- [ ] Fix file upload extension — whitelist hard-coded — DEV
