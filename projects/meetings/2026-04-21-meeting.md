# Meeting — 2026-04-21
**Agenda**: Semak semua aspek sistem e-Aduan v3
**Agents Hadir**: DEV, SECURITY, OPS, QA, DESIGN, DOC, PM

## Input Agents

### DEV
- Flow 3-langkah (borang → info_aduan → simpan) bersih
- 13 AJAX endpoints cascade dropdown — modular
- FLAG: `simpan.php:265` — race condition pada `SELECT MAX(wr_id)`. Patut guna `SCOPE_IDENTITY()`
- FLAG: Tiada CSRF token pada mana-mana form
- FLAG: `wr_photos` table auto-create dalam production code — patut satu kali masa deployment

### SECURITY
- Prepared statements konsisten — SQL injection dilindungi
- CSP headers, HSTS, X-Frame-Options aktif
- FLAG (KRITIKAL): `config/database.php:7` — DB password hardcoded
- FLAG (KRITIKAL): `config/auth.php:9` — JWT secret hardcoded
- FLAG (KRITIKAL): `config/auth.php:17` — Google OAuth client secret hardcoded
- FLAG: `sso_callback.php:46` — info disclosure via `die()` pada auth failure
- FLAG: CSP `unsafe-inline` melumpuhkan perlindungan XSS
- FLAG: `email_embom.php:116` — link emel guna `http://` bukan `https://`
- FLAG: Tiada CSRF protection

### OPS
- DB driver failover (pdo_sqlsrv → pdo_odbc) — elok
- FLAG: `config/auth.php:22` — `base_url` masih dev server, production dicomment
- FLAG: `google_redirect_uri` masih ke dev server
- FLAG: Tiada `.env` / config separation antara dev/prod

### QA
- JS validation cover semua field wajib
- 14 status code di semak.php — komprehensif
- FLAG: Pelajar/pengguna luar tiada pilihan bilik sendiri — by design?
- FLAG: Email failure senyap — tiada notifikasi admin
- FLAG: `upload_foto.php` belum disemak validationnya
- FLAG: Session expiry tidak dihandle gracefully

### DESIGN
- Branding UiTM konsisten, Bootstrap 5.3 custom
- Responsive, split-screen login
- FLAG: Tiada loading state pada dropdown — boleh submit sebelum data loaded
- FLAG: Map tile attribution salah (Google tile → atribute UiTM)

### DOC
- `/docs/` ada markdown asas
- `/daily-diary/` aktif
- FLAG: Tiada API doc untuk 13 AJAX endpoints
- FLAG: Tiada changelog formal

### PM
- Fungsi utama lengkap: submit, semak, detail, maklumbalas, daftar luar
- FLAG: 3 isu kritikal perlu resolved sebelum go-live
- FLAG: Config switch dev→prod masih manual

## Keputusan
Sistem tidak ready untuk production dalam keadaan semasa. 3 isu kritikal mesti diselesaikan dahulu.

## Action Items
- [ ] Tukar `SELECT MAX(wr_id)` kepada `SCOPE_IDENTITY()` di `simpan.php:265` — DEV (KRITIKAL)
- [ ] Tambah CSRF token pada semua form POST — DEV + SECURITY (KRITIKAL)
- [ ] Pindah credentials DB, JWT secret, Google OAuth ke `.env` atau env variable — OPS + SECURITY (KRITIKAL)
- [ ] Betulkan `email_embom.php:116` — tukar `http://` ke `https://` — DEV (TINGGI)
- [ ] Betulkan attribution peta — DESIGN (SEDERHANA)
- [ ] Tambah disable state butang semasa dropdown loading — DESIGN (SEDERHANA)
- [ ] Semak `ajax/upload_foto.php` — validation fail — QA + SECURITY (TINGGI)
- [ ] Wujudkan `.env.example` dan SOP switch dev→prod — OPS + DOC (TINGGI)

## Perlu Keputusan Abam
1. Credentials hardcoded dalam shared folder — boleh guna `.env` atau environment variable Windows/IIS?
2. Ada timeline tertentu untuk deployment ke production?
