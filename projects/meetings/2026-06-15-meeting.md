# XDIBAX Innovation — Team Meeting

**Tarikh:** 2026-06-15  
**Chair:** DIBA (COO)  
**CEO:** Zuex  
**Agenda:** Baik taraf War Room — info card auto-hide, 3D game feel, Claude terminal, embed chat  
**Hadir:** NEXUS, FORGE, LENS, ORACLE, PIXEL, ECHO, CIPHER, GRID, PULSE, SAGE

---

## Input Agents (ringkas)

| Agent | Status | Input | Flag |
|-------|--------|-------|------|
| NEXUS | War Room Three.js + Express port 3000 stabil | Pisah hover-card vs click-pin; endpoint `/api/open-ide` untuk spawn terminal | `main.js` 4500+ baris — pecah modul sebelum feature besar |
| FORGE | `/api/active-agents` baca transcripts Cursor — chat bubble NPC sudah sync | Embed chat guna transcript stream sedia ada, bukan spawn Claude CLI penuh | Diary 2026-06-09: `/api/chat` dibuang — perlu keputusan Zuex untuk revive ringan |
| LENS | Agent models + skill-log panel hidup | KPI embed: sesi aktif, model usage, task progress dalam chat dock | — |
| ORACLE | War Room = command center visual | Fasa 1 UX polish dulu; embed chat = differentiator fasa 2 | — |
| PIXEL | City pack + Kenney modular buildings ada; voxel zip blocked UiTM filter | Toon shading, outline pass, saturated palette, isometric camera preset | Asset zip perlu mirror luar campus |
| ECHO | Copy chat dock: "DIBA Command" bukan generic chat | Bubble NPC kekal pendek; panel chat untuk arahan Abam | — |
| CIPHER | `/api/open-folder` whitelist `C:\Users\BSM\` — kekalkan | Terminal spawn hanya path verified; jangan expose API key dalam browser | — |
| GRID | `vscode://` / `cursor://` file URI sudah ada di projects panel | Tambah `POST /api/open-claude-terminal` via `wt` atau IDE CLI | Perlu verify `claude` + `cursor` dalam PATH |
| PULSE | Hover detection setiap 3rd frame; skill-card click-to-pin | Test: card hilang ≤300ms selepas mouseleave; no jitter pada drag camera | — |
| SAGE | Referensi: SimCity build mode, RimWorld HUD auto-fade | Post-processing ringan (bloom) untuk game feel tanpa berat GPU | — |

---

## Keputusan Operasi

1. **Info card auto-hide** — Hover = preview card (skill/building); mouseleave + 400ms delay → fade out. Click = pin (kekal); click luar / Esc = tutup.
2. **3D game feel (Fasa 1)** — Aktifkan shadow map, toon-ish material tweak, outline pada NPC, camera preset isometric, lebih guna modular Kenney buildings dalam auto-city. Fasa 2: post-processing bloom.
3. **Buka Claude terminal** — Butang `CLAUDE` di projects panel → `POST /api/open-claude-terminal` { path, ide: cursor|vscode } → spawn Windows Terminal `cd path && claude` atau fallback `cursor path`.
4. **Embed chat** — Panel dock bawah kanan `#chat-dock`: stream dari `/api/active-agents` + input arahan (log ke diary, bukan full Claude API tanpa key). Full interactive chat = escalate Zuex.

---

## Action Items

| # | Task | Owner | Fail |
|---|------|-------|------|
| 1 | Hover preview + auto-hide skill-card | PIXEL + PULSE | `war-room/public/main.js`, `index.html` |
| 2 | Shadow + material + isometric preset | PIXEL + NEXUS | `main.js` |
| 3 | `/api/open-claude-terminal` + butang CLAUDE | GRID + CIPHER | `server.js`, projects panel |
| 4 | `#chat-dock` embed transcript stream | FORGE + ECHO | `index.html`, `main.js`, `server.js` |
| 5 | Verify PATH claude/cursor/wt | GRID | manual test Windows |

---

## Perlu Keputusan Zuex

1. **Revive chat API?** — 2026-06-09 dibuang; embed dock transcript-only vs full Claude interactive.
2. **Prioriti fasa** — Semua 4 serentak atau Fasa 1 (card + 3D) dulu?

---

*Minit oleh DIBA — War Room Upgrade Sprint*
