# DIBA Session Diary - September 15, 2026

## ScoutOps — Full Security & Architecture Audit

**Context**: Abam suruh scan + audit penuh repo ScoutOps (`https://github.com/xdaxzurairi/scoutops.git`). Tracker lama (`active/scoutops/index.md`, ditulis 2026-07-15) kata "0% completion, tiada auth" — sangat stale. Realiti: 79 PR merged, RBAC multi-tenant, deployed live kat Vercel.

**What was done**:
- Clone fresh ke `C:/Users/Administrator/xdibax/scoutops` (path lama `C:/Users/BSM/...` tak wujud kat mesin ni).
- Audit: auth/session/CSRF stack, SQL injection, SSRF, XXE, file upload handling, multi-tenant org-scoping (IDOR), secrets/.gitignore, Vercel routing exposure, CSV export, rate limiting.
- Verified fix-points instead of assuming — contoh: baca `build_report()` sendiri untuk confirm org-isolation betul-betul dikuatkuasakan, bukan just decorative parameter.

**Findings**: 0 HIGH, 3 MEDIUM, 3 LOW. No code changes made — audit only.
- MEDIUM: `config/auth.php` dead code masih simpan real bcrypt hash untuk akaun admin `xdaxzurairi` dalam git history (tak web-reachable, tapi patut delete + rotate password).
- MEDIUM: Org isolation 100% app-layer — Postgres RLS disabled schema-wide (`sql/007_disable_rls_session_captures.sql`). Semua query yang disemak betul, tapi tiada DB-level backstop.
- MEDIUM: Rate limit login + NRIC-unlock (`my_report.php`) per-IP sahaja — boleh bypass dengan rotate IP, terutama 4-digit NRIC gate (10k kombinasi).
- LOW: CSV export formula-injection risk, XLSX import tiada size guard, tiada security headers (CSP/HSTS/X-Frame-Options) langsung.
- Good practices confirmed: bcrypt, CSRF hash_equals, parameterized queries, session fixation protection, high-entropy share tokens, server-side MIME sniffing, SSRF sudah host-allowlisted pada Sheets sync.

**Decision**: Update tracker terus (bukan tunggu approval) — betulkan stale "0%/no auth" jadi status sebenar + findings di atas. Reorder LRU: ScoutOps naik ke pos #1 (2026-09-15).

**Next steps**: Abam decide bila nak fix 3 MEDIUM items (hash rotation + delete config/auth.php ada priority tertinggi sebab credential hygiene). LOW items boleh batch satu cleanup PR.

**Note**: Telegram diary bridge (`scripts/send-diary-telegram.js`) masih belum wujud dalam repo — skip send, macam yang dah dicatat dalam reminders.md sebelum ni.

---

## Evening Update

### Moonshot/Kimi Hijack — Dibersihkan

**Apa berlaku**: `~/.claude/settings.json` ada `env` block yang redirect semua Claude Code sessions ke `api.moonshot.ai/anthropic` — model jadi `kimi-k3[1m]`, bukan Anthropic betul. API key Moonshot tersimpan plaintext.

**Tindakan**: Buang 8 key Moonshot dari `env` block. Kekal `CLAUDE_CODE_AUTO_COMPACT_WINDOW` + `CLAUDE_CODE_EFFORT_LEVEL` (legit). Tidak auto-commit (repo track `~/.ssh/` + credentials — Abam kena confirm dulu).

**Action item untuk Abam**: Revoke Moonshot API key di Moonshot console (manual login diperlukan).

### Open Items (carry forward)
- ScoutOps: 3 MEDIUM items tunggu Abam decide priority (hash rotation paling urgent)
- Moonshot key revocation: Abam perlu buat secara manual
- Telegram bridge + L4 Gmail/Calendar MCP: masih belum wujud / belum authenticate
- Ollama lokal PC opis: overdue 2 bulan
