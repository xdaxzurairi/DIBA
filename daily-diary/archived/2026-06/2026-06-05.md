# Diary — 2026-06-05

## Kerja Hari Ini

### Projek: eWorks PWA — DB Trigger Investigation & PROD Restore

**Topik: Semak triggers database dan restore PROD**

#### 1. Analisa Trigger `UPDATE_WR_WRCF`
- Disemak definisi trigger pada jadual `afm.wr` di DEV
- Trigger ni AFTER UPDATE — sync `date_completed` & `time_completed` dari `wrcf` ke `wr` untuk `PREVENTIVE MAINT` sahaja
- Confirm: tiada impact pada INSERT aduan baru (corrective) melalui PWA eWorks
- Noted: INSERT menggunakan `SELECT MAX(wr_id)` workaround sebab trigger halang `OUTPUT` clause

#### 2. Compare DEV vs PROD Triggers
- Run query `sys.triggers` pada kedua-dua DB
- **Dapatan kritikal**: 5 triggers diubah hari ini (2026-06-05) kat PROD — `WR_INSERT`, `WR_UPDATE`, `WO_INSERT`, `WO_UPDATE`, `system_status_t`
- 4 triggers PROD lebih lama dari DEV: `UPDATE_WR_WRCF`, `WR_DATE_TRIG`, `WR_DELETE`, `WR_STATE_SITE`
- 1 trigger missing kat PROD: `tr_rn_daftar_cabutan_lock_kategori`

#### 3. Restore PROD Triggers
- Generated `scripts/restore_triggers_prod.sql` — 10 ALTER + 1 CREATE
- Created `scripts/run_restore.php` — browser-based SQL runner dengan token security
- Tukar `secrets.php` sementara ke PROD (`10.0.36.128 / FMSPROD`)
- Run restore → **10/11 berjaya**, 1 skip (`tr_rn_daftar_cabutan_lock_kategori` — table `rn_daftar_cabutan` tak wujud kat PROD)
- Revert `secrets.php` balik ke DEV
- Padamkan `run_restore.php` selepas selesai

#### Keputusan
- PROD triggers untuk `wr` dan `wo` kini selaras dengan DEV
- `tr_rn_daftar_cabutan_lock_kategori` tangguh — deploy bila table `rn_daftar_cabutan` ada kat PROD
- Scope semak: triggers sahaja (tables, SP, views belum disemak)

## Fail Berkaitan
- `scripts/restore_triggers_prod.sql` — SQL restore script (kekal sebagai rekod)
- `config/secrets.php` — connection config (semasa: DEV)

## Langkah Seterusnya
- Monitor PROD kalau ada isu berkaitan `wr`/`wo` audit log
- Deploy `tr_rn_daftar_cabutan_lock_kategori` bila PROD ready
